<div class="row align-center mb-5">
  <a href="/blogs/{{id}}/comment">
    <div class="row align-center mb-5">
      <div class="col-md-6">
        <h2>{{name}}</h2>
        <h2>{{title}}</h2>
        <h2>{{description}}</h2>
      </div>
      <div class="col-md-6">
        <h3>{{date}}</h3>
      </div>
    </div>
  </a>
</div>
    {{#each comments as |comment|}}
      {{> comment-details}}
    {{/each}}
